# UZ Bookshelf サービス批評

**対象**: https://nekos-git.github.io/nekos/
**レビュー日**: 2026-02-18

## 概要

GitHub Pages で公開されている **「UZ Bookshelf」** は、本・マンガ・映画・音楽のコレクションを擬似3D本棚UIで閲覧できる React ベースの SPA（シングルページアプリケーション）です。UZ Media というメディアサイトの記事と連動し、各作品にAmazon/楽天へのリンクを提供するキュレーション型の書棚サービスです。

### 技術スタック
- React 18（CDN経由、プロダクションビルド）
- Babel Standalone（ブラウザ内JSXトランスパイル）
- 楽天Books API データ（静的JSON化）
- GitHub Pages ホスティング（`/docs` ディレクトリ配信）

---

## 良い点

### 1. ビジュアルデザインの完成度が高い
- CSS Transform を使った擬似3D表現（`perspective`, `rotateY`, `rotateX`）で本棚の奥行き感を実現
- 表紙・背表紙・ページ断面・光沢・影の5層構造で、リアルな書籍表現を達成
- 判型（文庫・コミック・新書・単行本・ハードカバー・ディスク）ごとにサイズが異なり、リアリティがある
- タイトル文字列からハッシュ値を生成し、HSL色空間で背表紙の色をバリエーション豊かに自動生成（`stableHash` → `spineColorFromTitle`）

### 2. 豊富なインタラクション
- ホバー時のツールチップ（350ms遅延で誤発火防止）
- モーダルでの詳細表示（表紙画像、著者、出版社、価格、レビュー星数、記事リンク）
- 記事一覧パネルでの検索・フィルタリング機能
- 棚のページネーション（前後ナビ）

### 3. レスポンシブ対応が細かい
- 768px（タブレット）、600px（スマホ）、400px（小型スマホ）の4段階ブレークポイント
- `@media (hover: none)` でタッチデバイスのツールチップを無効化する配慮
- 各ブレークポイントで3D変換の `scale` と `perspective` を段階的に調整

### 4. コンテンツの充実
- 小説・文学（村上春樹、カフカなど）、マンガ・アニメ、映画・DVDの3棚構成
- 楽天Books API からテクノロジー、ビジネス、カルチャーの3棚を追加表示
- 記事との紐付けにより「なぜこの本を推薦するのか」の文脈が提供されている
- 表紙画像は約280枚をローカルにダウンロード・ハッシュ化して`covers/`に格納（外部画像依存を削減）

### 5. 適切なHTMLメタ構造
- `<!DOCTYPE html>` 宣言あり
- `lang="ja"` で言語指定
- `viewport` メタタグあり
- `meta description` あり

---

## 問題点・改善提案

### 致命的な問題

| # | 問題 | 深刻度 | 詳細 |
|---|------|--------|------|
| 1 | **ブラウザ内Babelトランスパイル** | 高 | `<script type="text/babel">` でブラウザ実行時にJSXをトランスパイルしている。Babel Standalone（約800KB）のダウンロード＋パース＋トランスパイルが毎回発生し、初期表示が大幅に遅延する。プリビルドに移行すべき |
| 2 | **表示が真っ白になる可能性** | 高 | ReactアプリがJSで描画するため、Babel/React/ReactDOMの読み込みに失敗すると `<div id="root"></div>` だけの白画面になる。実際にWebFetchで確認したところ、ページタイトル「UZ Bookshelf」のみ表示され本文は空だった。SSRやローディング表示が必要 |
| 3 | **SRI (Subresource Integrity) 未使用** | 高 | unpkg.com から3つのスクリプト（React、ReactDOM、Babel）を読み込むが、`integrity` 属性がない。CDN改竄時に悪意あるコードが実行されるリスク |

### パフォーマンスの問題

| # | 問題 | 詳細 |
|---|------|------|
| 4 | **Babel Standalone のオーバーヘッド** | ~800KBのBabelバンドル + 実行時トランスパイルのCPUコスト。Vite等でプリビルドすれば不要 |
| 5 | **画像の遅延読み込み未実装** | 約280枚の表紙画像が一括読み込みされる。`loading="lazy"` や Intersection Observer を使った遅延読み込みが必要 |
| 6 | **JSONデータの並列fetch** | 6つのJSONファイル（書籍3 + ジャンル3）を `Promise.all` で並列取得しているが、ジャンル完了後に書籍を取得する依存構造のため、実質的にウォーターフォール。初期データをバンドルに含めるか、1つのJSONに統合すべき |

### セキュリティの問題

| # | 問題 | 詳細 |
|---|------|------|
| 7 | **外部CDN依存（integrity無し）** | React, ReactDOM, Babel の3ライブラリすべてがunpkg.comに依存。SRIハッシュがないため改竄検知不可 |
| 8 | **アフィリエイトURLの直接埋め込み** | 楽天アフィリエイトURLがJSONデータにハードコードされており、URLが変更された場合の一括更新手段がない |
| 9 | **XSS考慮** | ReactのJSXが自動エスケープするため現時点でのリスクは低いが、`dangerouslySetInnerHTML` 相当の処理はないことを確認済み。ただしJSONデータが外部ソースの場合、入力検証がない |

### UX の問題

| # | 問題 | 詳細 |
|---|------|------|
| 10 | **初期ローディング状態の欠如** | データ取得・Babelトランスパイル中に何も表示されない。スケルトンUIやスピナーが必要 |
| 11 | **エラー状態の欠如** | fetch失敗時に `console.error` のみ。ユーザー向けのエラーメッセージやリトライUIがない |
| 12 | **キーボードナビゲーション未対応** | 本のクリックが `<a href="#">` で実装されているが、`onClick` で `preventDefault` しており、実質的にボタンとして機能。`role="button"` や `tabIndex` の適切な設定が必要 |
| 13 | **アクセシビリティ不足** | 表紙画像の `alt` が空文字列。モーダルに `role="dialog"` や `aria-modal` がない。フォーカストラップ未実装 |
| 14 | **URL状態管理がない** | 棚の選択やモード切替がURLに反映されないため、ブックマークや共有ができない。`history.pushState` やハッシュルーティングが必要 |

### コード品質の問題

| # | 問題 | 詳細 |
|---|------|------|
| 15 | **単一ファイル肥大化** | `UzBookshelf.jsx` に全コンポーネント・ユーティリティ・データ処理が約380行に詰め込まれている。コンポーネント分割（BookFace, BookSpine, Modal, ArticlesPanel等）が必要 |
| 16 | **IDの重複** | `uz-shelf-data.json` 内で同一 `articleId` のアイテムが大量に存在（例: `murakami-haruki-sotsugyou` が5回以上）。React の `key` prop にも影響する可能性がある |
| 17 | **テスト未整備** | 自動テストが一切存在しない。ユーティリティ関数（`stableHash`, `detectFormat` 等）は単体テストが容易なのに未実施 |
| 18 | **ビルドパイプライン未整備** | CI/CDがなく、手動デプロイのみ。GitHub Actions でビルド→デプロイの自動化が望ましい |

### プロジェクト構造の問題

| # | 問題 | 詳細 |
|---|------|------|
| 19 | **デプロイブランチの異常** | `main` ブランチにはARデモしかなく、実際のサービスは `claude/explore-book-branch-1GwKc` という作業ブランチからデプロイされている。正式ブランチ（`main` または `gh-pages`）にマージすべき |
| 20 | **リポジトリの役割混在** | 同一リポジトリに ARデモ（test/）、本棚アプリ（docs/）、小説テキスト（end2endworld/novel/）、ひらがなパーティ（hiragana_party/）、MCPメモリサーバ（mcp_memory_server.py）など、無関係なプロジェクトが混在 |
| 21 | **README が無関係** | `main` ブランチの README は `# nekos` の1行のみで、UZ Bookshelf についての説明は一切ない |
| 22 | **`memory.db` がリポジトリに含まれている** | SQLiteデータベースファイルがバージョン管理されている。`.gitignore` に追加すべき |

---

## 総合評価

### 点数: 55 / 100

**UZ Bookshelfは、ビジュアル面で印象的な個人プロジェクトです。** 擬似3D本棚の表現は CSS Transform の巧みな活用で美しく、判型ごとのサイズ差異、ハッシュベースの背表紙色生成など、細部へのこだわりが見られます。コンテンツのキュレーション（村上春樹からスタジオジブリまで）にもセンスがあります。

しかし、**サービスとしての信頼性に重大な問題**があります:

1. **ブラウザ内Babelトランスパイルは本番環境では許容されない** — 初期表示の遅延と、トランスパイル失敗時の白画面リスクは、ユーザーが離脱する最大の原因になる
2. **作業ブランチからの直接デプロイ** — プロジェクト管理として不安定で、意図しないコードが公開されるリスクがある
3. **エラー時のフォールバック欠如** — ネットワーク障害やCDN障害で完全に動作不能になる

### 改善の優先順位

| 優先度 | 対応 |
|--------|------|
| **P0** | Vite等でプリビルドに移行し、Babel Standaloneを排除 |
| **P0** | `<div id="root">` 内にローディングUIまたはnoscript表示を追加 |
| **P0** | デプロイを正式ブランチ（`main` or `gh-pages`）に移行 |
| **P1** | SRI ハッシュの追加（またはライブラリをローカルバンドル） |
| **P1** | 画像の遅延読み込み実装 |
| **P1** | fetch エラー時のユーザー向けフォールバックUI |
| **P2** | コンポーネント分割とテスト追加 |
| **P2** | アクセシビリティ改善（ARIA属性、キーボードナビゲーション） |
| **P2** | URL ベースの状態管理（ブックマーク対応） |
| **P3** | リポジトリ整理（無関係プロジェクトの分離） |

### 結論

ビジュアルのセンスとフロントエンドCSS技術力は高い水準にあります。しかし「プロダクション品質」と「プロトタイプ品質」の間にある壁——ビルドパイプライン、エラーハンドリング、アクセシビリティ、プロジェクト管理——を越えるための作業が残っています。最も投資対効果の高い改善は **Babelランタイムの排除とプリビルド化** であり、これだけで初期表示速度とサービス信頼性が劇的に改善します。
